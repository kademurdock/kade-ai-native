# iPhone character reactions — Part 175

Kiana and Della retain their original portraits and exact avatar filename gates. Six registered expression cells add amused, serious, concerned, skeptical, surprised and warm faces. Only facial regions blend, over the original background and clothing. Existing mouth and blink patches remain. Authored leading `%%%direction%%%` tags supply expression; ordinary prose is never classified as emotion. Leading laugh/gasp/sigh cues are brief overlays. Interior directions have no invented audio timestamps and are not applied early.

Buffered and streamed voice replies take their cue from the item that actually begins playing. Pausing, interruption and changed identity clear animated motion. Calls accept optional version-1 `character-audio` metadata immediately before a WAV. The packet carries speaker ID, speech/non-speech, expression and optional moment; no transcript. The production audio scheduling, microphones, synthesis and output bytes remain authoritative. A separate bounded presentation queue samples the existing player's source clock, and actual output amplitude drives the mouth. Server state changes cannot cut off a locally playing tail. Missing/invalid metadata, game sounds and another speaker leave the mouth closed.

The same single decorative view loads the authenticated agent roster when needed, including calls opened without visiting the chat picker. Reduce Motion, hidden/background, low power, offscreen and saved portrait controls remain. VoiceOver gets no new focus stops or frame announcements. The original source held by Part 173 is not part of this branch.

Run `bash run-speech-tests.sh`, `bash run-character-tests.sh`, and the bridge's `node --test character-audio.test.js voice-stream.carry.test.js voice-stream.part110.test.js`. Shared cue fixtures must agree between Swift and JavaScript. `KADE_CHARACTER_AUDIT=1` enables an isolated DEBUG simulator acceptance gate in the TestFlight workflow: it renders the actual portrait component and exercises the real AVAudioPlayer and StreamingCallService output paths with local test tones. No live user, sign-in, microphone, provider request or conversation is created. That fixture is absent from Release.

Artwork masters and exact built-in image-generation prompts are under the context folder's `masters/characters-2026-09-09-part175`. `registration.json` documents mechanical alignment to the unchanged originals. These are expressive 2D portraits with audio-amplitude mouth movement; no phoneme recognition, arbitrary expression inference or articulated 3D body is claimed. Physical-device VoiceOver, Bluetooth latency and battery acceptance remain separate from simulator tests.
