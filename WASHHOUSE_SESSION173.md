The Washhouse picture update is prepared on session173-washhouse, outside main.

WorldRoom forwards the optional public washhouse.benchStage field into WorldPictureRoom. Older payloads remain decodable. The exported offline scene uses pale green linoleum, a blue button tin, book shelf and window bench, and removes its repair tray when the three shared repairs are complete. Commands, choices and story text continue to arrive from the server without a new native build.

Validation: 19 Foundation model checks, WorldService Swift syntax parsing on the existing Linux devbox, and offline browser rendering. These do not establish an iOS archive, physical VoiceOver acceptance, or device performance. No Mac build was started; main and TestFlight 282 remain unchanged. Export with `node LibreChat/dev/reverie/export-native.cjs kade-ai-native` from the session work directory after any web scene change.
